para que esto funcione se debe instalar 
typescript
@types/express tipos de dato de express
ts-node-dev
y crar los suguientes scripts
  "scripts": {
    "dev": "ts-node-dev src/app.ts",
    "tsc": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
